{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": null,
  "installCommand": "corepack enable && cd frontend && YARN_PRODUCTION=false yarn install --frozen-lockfile",
  "buildCommand": "corepack enable && cd frontend && GENERATE_SOURCEMAP=false yarn build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
